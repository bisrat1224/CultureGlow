import { PageStub } from "@/components/PageStub/PageStub";

// Real content (masonry photo grid, lightbox, TikTok video grid) is out
// of scope for this build — comes in the Gallery content chunk.
export default function GalleryPage() {
  return <PageStub title="Gallery" />;
}